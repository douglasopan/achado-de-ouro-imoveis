const MapPlaceholder = () => {
  return (
    <div className="rounded-lg border bg-muted/30 p-6 text-sm text-muted-foreground">
      Mapa interativo será exibido aqui. Integração Mapbox será ativada após conexão do backend (Supabase) e configuração do token público.
    </div>
  );
};

export default MapPlaceholder;
